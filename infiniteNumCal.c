#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "infiniteNumberNode.h"

int main() {
    char ch;
    Node *head, *dot, *tail;

    while ((ch = getchar() != '\n')) {
        Node *temp = (Node *)malloc(sizeof(Node));
    }
}
