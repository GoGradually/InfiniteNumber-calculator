#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "infiniteNumberNode.h"

int main() {
    char ch;
    NumberList head, tail, dot;

    while ((ch = getchar() != '\n')) {
        if(ch >= '0' && ch <= '9'){

        }else if(ch == '*')
        Node *temp = (Node *)malloc(sizeof(Node));
    }
}
